# SAMPLE cards — verify the import/update round-trip, then delete this file
# once real content lands. (Deleting the file removes cards from future
# builds; already-imported notes stay in Anki until deleted there.)
deck: Interview Prep::Samples
cards:
  - id: sample-basic-01
    type: basic
    front: "[sample] What dominates GPU memory in LLM serving?"
    back: The KV cache.
    tags: [sample]

  - id: sample-cloze-01
    type: cloze
    text: "[sample] Prefill is {{c1::compute}}-bound; decode is {{c2::memory-bandwidth}}-bound."
    tags: [sample]

  - id: sample-code-01
    type: code
    front: "[sample] Get the k smallest items from a list, idiomatically."
    back: |
      import heapq
      smallest = heapq.nsmallest(k, xs)
    tags: [sample]
